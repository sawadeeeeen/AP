# AP試験 SRS学習ガイド（Claude向け）

このリポジトリはITパスポート・基本情報技術者（AP）試験の学習を
間隔反復（SRS / SM-2アルゴリズム）で支援するシステムです。

## セットアップ

```bash
python db.py init
```

初回のみ実行してください。SQLiteのDB（`ap_study.db`）と問題データが作成されます。

---

## 学習セッションの進め方

### ステップ1: 今日の復習数を確認

```bash
python db.py due
```

### ステップ2: 次の問題を取得

```bash
python db.py next
# 特定分野だけ学習する場合
python db.py next セキュリティ
python db.py next データベース
```

出力例:
```json
{
  "id": 37,
  "category": "セキュリティ",
  "question": "SQLインジェクション攻撃への対策として最も効果的なものはどれか。",
  "choices": {
    "1": "入力値の文字数を制限する",
    "2": "プリペアドステートメント（バインド変数）を使用する",
    "3": "エラーメッセージを詳細に表示する",
    "4": "パスワードをMD5でハッシュ化する"
  }
}
```

### ステップ3: ユーザに選択肢を提示して回答を受け取る

Claudeは問題のJSONを受け取ったら、
選択肢（1〜4）をユーザに提示してください。
ユーザが回答番号を入力したら次のステップへ。

### ステップ4: 正誤を記録

```bash
# 正解の場合
python db.py answer <id> 1

# 不正解の場合
python db.py answer <id> 0
```

出力に含まれる `explanation`（解説）と `correct_answer` をユーザに伝えてください。

### ステップ5: 繰り返す

`due_today > 0` の間はステップ2〜4を繰り返します。
`status: "done"` になったら本日の学習完了です。

---

## コマンドリファレンス

| コマンド | 説明 |
|---|---|
| `python db.py init` | DB初期化（初回のみ） |
| `python db.py next [分野]` | 次の問題をJSON出力 |
| `python db.py answer <id> <1\|0>` | 正誤記録・SRS更新 |
| `python db.py due` | 今日の復習件数 |
| `python db.py stats` | 分野別正答率 |
| `python db.py categories` | 分野一覧 |

---

## 分野一覧

```bash
python db.py categories
```

| 分野 |
|---|
| 基礎理論 |
| アルゴリズムとプログラミング |
| コンピュータ構成要素 |
| システム構成要素 |
| ソフトウェア |
| ハードウェア |
| 人間とコンピュータの対話 |
| マルチメディア |
| データベース |
| ネットワーク |
| セキュリティ |
| システム開発技術 |
| ソフトウェア開発管理技術 |
| プロジェクトマネジメント |
| サービスマネジメント |
| システム戦略 |
| 経営戦略マネジメント |
| 技術戦略マネジメント |
| ビジネスインダストリ |
| 企業と法務 |

---

## SRSの仕組み

SM-2アルゴリズムに基づく間隔反復：

- **正解**するたびに次の復習間隔が伸びる（1日→6日→徐々に延長）
- **不正解**の場合は翌日に再出題
- 各問題の「覚えやすさ係数（EF）」が正誤に応じて動的に調整される

---

## 注意事項

- `ap_study.db` はGitignore済み（学習履歴はローカル保持）
- 問題数: 80問 / 20分野
- 問題の追加は `questions.py` の `QUESTIONS` リストに追記するだけ
